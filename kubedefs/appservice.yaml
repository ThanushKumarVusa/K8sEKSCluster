apiVersion: v1
kind: Service
metadata:
  name: vproapp
  labels:
    app: vproapp
spec:
  selector:
    app: vproapp     # ✔ ONLY THIS
  ports:
    - protocol: TCP
      port: 8080
      targetPort: vproapp-port
  type: ClusterIP     # ✔ Move this OUTSIDE selector
